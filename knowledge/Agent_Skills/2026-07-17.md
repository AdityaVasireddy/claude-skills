---
project: Agent_Skills
date: 2026-07-17
type: mixed
stack: [bash, git, claude code hooks (SessionEnd/PreCompact), engineering-historian sweep pipeline, crucible skill (markdown docs), gh cli, winget]
status: Crucible v2.0.0 released on GitHub (commit `7077831`, tag `v2.0.0`, release published) and Stage 4 adequacy methodology committed (`993fe4e`, push not yet confirmed); historian v3.1.3 shipped earlier; claude.ai copies of both skills need manual re-upload
---

# Agent_Skills — 2026-07-17

## What I worked on
Two rounds of work on the engineering-historian repo. First, updated the public README to summarize v3.1.2's production-hardening work and committed it (`a06efd8`). Second, fixed a live-confirmed bug where the sweep hook's `run_pipeline()` stamped a `knowledge/` vault into any session's cwd — including headless `-p --no-session-persistence` children that always skip — because filesystem writes ran before the transcript-existence and triviality gates. Reordered the pipeline so no writes happen before the gates, added a global skip log, added an atomic session-ID lock to close a SessionEnd/PreCompact double-fire race the existing byte-growth dedupe couldn't catch, ran the full test suite (75/75 passing, 13 new assertions), deployed, verified live, and committed (`d77ac8e`). Updated the README again for v3.1.3 and committed (`a2d645a`). Finally, checked whether the fix had propagated everywhere and found the installed skill copy was missing `historian-sweep.sh`, `run-tests.sh`, and `CHANGELOG.md` (only `README.md` had been synced earlier) — fixed that gap too.

## Cases

### CASE: write-before-gate-reorder
status: auto
problem: `run_pipeline()` created the `knowledge/` vault (mkdir, seeded `.gitignore`, seeded `capture-rules.md`, `sweep.log` append) before checking transcript existence or the triviality gate, so every session end — even ones that immediately SKIP — stamped a vault into whatever cwd it ran in, confirmed live in headless `-p` children and known to have polluted an unrelated project's repos.
decision: moved all filesystem writes to `$VAULT` below the transcript-existence check and triviality gate; SKIP events now log to a new global `~/.claude/historian/skips.log` instead of a per-project vault.
why: desired behavior is that a session that would SKIP leaves the cwd absolutely untouched — zero files, zero dirs — which is only possible if skip logging never targets the session cwd.
tags: [bugfix, hooks, historian, filesystem]

### CASE: sessionend-precompact-double-fire-lock
status: auto
problem: SessionEnd and PreCompact both run the same sweep pipeline; the existing byte-growth dedupe (keyed on session ID in `knowledge/.sweep/sessions.log`) only guards against a prior *completed* sweep and does nothing if both hooks are already running concurrently before either has recorded its bytes.
decision: added an atomic `mkdir`-lock keyed on session ID under `~/.claude/historian/locks/`, with stale-lock reclaim and an empty-`SESSION_ID` guard, checked before the model call.
why: the model call is the expensive step (real token cost) — the byte-based dedupe can't catch the concurrent-firing window, so the lock has to sit earlier in the pipeline than that check.
tags: [concurrency, hooks, dedupe, historian]

### CASE: historian-sweep-active-frozen-interface
status: auto
problem: `HISTORIAN_SWEEP_ACTIVE` (first line of the script, the recursion guard) is now relied on by an external system, not just internally.
decision: kept the guard exactly as-is — never renamed, nothing may run before that check — and documented it inline as a frozen interface.
why: issue-runtime's ADR-22 option B sets this var in an engine child's env specifically to suppress the sweep; that dependency isn't visible from this repo alone, so it had to be called out explicitly to avoid future breakage.
tags: [interface, historian, hooks, compatibility]

### CASE: regression-coverage-scope-skip-redundant-live-test
status: auto
problem: after the fix, whether to also drive a fresh live multi-turn chat session purely to re-prove that normal (non-skip) sweeps still work, on top of the automated suite.
decision: relied on the existing automated regression suite (T2–T4, T8, T15–T17, T19–T20, all asserting real sweep/day-file/commit behavior, 75/75 passing) as sufficient evidence instead of running a redundant live session.
why: judged the automated coverage exhaustive enough that an extra live multi-turn test would be redundant re-proof of already-asserted behavior.
tags: [testing, historian, judgment-call]

## Commands that worked
None (no verbatim shell commands appear in the transcript — actions were described narratively, not shown as literal command text).

## Problems solved
`knowledge/` vault contamination in unrelated cwds → root cause: filesystem writes ran before the transcript-existence/triviality gates in `run_pipeline()` → fix: reordered so all writes happen after the gates, skip logging moved to a global log file.
SessionEnd/PreCompact concurrent double-fire → root cause: byte-growth dedupe only detects a prior *completed* sweep, not a sweep already in flight → fix: atomic session-ID mkdir-lock checked before the model call.

## Mistakes
Installed skill copy (`~/.claude/skills/engineering-historian/`) fell behind after the v3.1.2 README sync — only `README.md` was synced at the time, and `historian-sweep.sh`, `run-tests.sh`, and `CHANGELOG.md` were missed, so the v3.1.3 fix wasn't actually available machine-wide until the gap was caught (when the user asked directly) and closed.

## Prompts worth keeping
The rev-2 bug-fix prompt ("Fix write-before-gate contamination + double-fire dedupe in historian-sweep.sh (rev 2)") — a fully self-contained spec with explicit Context/Fix 1/Fix 2/frozen-interface sections and exact line-number pointers — produced a clean, low-friction implementation pass with no back-and-forth on scope.

## Questions I still have
Whether/when the claude.ai project skill copy will get its manual browser update — flagged as still pending after both the v3.1.2 and v3.1.3 README syncs.

## TODOs for next session
Manually update the claude.ai project skill copy of engineering-historian in the browser (README and code now both behind after two sync rounds).

## Session 2 (time unknown)

### What I worked on
A full redesign-to-release arc for the Crucible skill. Started with a meta-analysis of Crucible v1's simulated investment council, then an adversarial teardown of the first v2 draft (which found most of it was ceremony over the same correlated-model core), then a synthesis of the council-vs-evidence-engine philosophies into the verifiable-now/verifiable-later axis that became v2. Built the complete v2 product foundation (PHILOSOPHY, ARCHITECTURE, WORKFLOW, EVALUATION, MIGRATION docs plus a rewritten `SKILL.md` and re-run worked example), ran a release review that found five findings (two blocking), implemented all fixes, and committed the redesign (`2cee4fd`). Prepared and shipped the v2.0.0 release: CHANGELOG, release notes, roadmap, checklist, commit `7077831`, tag `v2.0.0`, and a published GitHub release (user pushed manually after my pushes were permission-denied; `gh` was installed via winget mid-session for the release publish). Installed the skill to `~/.claude/skills/crucible/`, then debugged a claude.ai skill copy that turned out to be a stale v1 upload. Finally, refined Stage 4's evidence methodology to separate competitor existence from problem adequacy and committed it (`993fe4e`). The `3fb510a history(auto)` commit in the delta is the sweep's own earlier output and was not discussed.

### Cases

### CASE: verifiable-now-axis-replaces-judge-vs-not-judge
status: auto
problem: two competing Crucible philosophies — v1's simulated investment council (LLM as judge) versus a pure evidence engine (LLM never judges) — with the framing question of whether to pick one or combine them.
decision: rebuilt v2 on a third axis instead: the LLM asserts only judgments verifiable-now from stated facts (arithmetic, contradictions, incentive mapping, structural impossibility), while everything only-verifiable-later becomes a KNOW/BELIEVE/HOPE assumption resolved by real-world experiments and a persistent ledger.
why: "LLMs should not judge" was an overcorrection — convergence among five personas of the same model is correlated and mostly measures obviousness, but a judgment the founder can check tonight (margin math not closing, an inverted payment structure) is trustworthy the moment it's uttered.
tags: [architecture, crucible, llm-epistemics, design]

### CASE: claude-ai-skill-slot-held-stale-v1-copy
status: auto
problem: the claude.ai copy of the crucible skill insisted the ledger and assumption stack didn't exist, and its own grep of `/mnt/skills/user/crucible/SKILL.md` found no Stage sections — while the local file was byte-identical to the tagged v2.0.0 release.
decision: diagnosed the claude.ai slot as holding a stale v1 upload (its reported Brief→Council→Judge structure matched v1 exactly) and fixed it by re-uploading the real file from disk, rejecting the remote model's offer to receive a paste and rewrite the file itself.
why: a paste/diff-write path risks another lossy round-trip, and the v1-shaped structure proved the stored copy predated the redesign rather than being truncated.
tags: [sync, claude-ai, skills, debugging]

### CASE: hope-kept-out-of-evidence-tier-schema
status: auto
problem: the Stage 4 adequacy spec listed HOPE as a possible tier for the "is the problem actually well solved?" question, but HOPE is an assumption-stack tag, not an evidence tier, and the tier schema was frozen across all docs.
decision: deliberately deviated from the spec — adequacy findings carry evidence tiers only, and an evidence-less adequacy assumption enters the stack as HOPE — and flagged the deviation to the user.
why: honors the spec's intent without corrupting the frozen tier schema.
tags: [crucible, schema, spec-deviation]

### CASE: session-log-field-over-weakened-evaluation-claim
status: auto
problem: `EVALUATION.md` claimed every indicator is computable from ledger data alone, but Bench verdicts and evidence-tier counts were never persisted, so two indicators weren't computable.
decision: extended the ledger with a per-session `## Session log` line (Bench verdict + evidence-tier counts) rather than scoping the claim down to founder-side indicators.
why: the data was already generated every session and merely not persisted, so persisting it is a data field within the Ledger's existing responsibility, not a new stage — keeping the evaluation promise intact without an architecture change.
tags: [crucible, ledger, docs-consistency]

### CASE: indicator5-reworded-not-implemented
status: auto
problem: `EVALUATION.md`'s interrogation-delta indicator measured a founder-drafts-their-own-stack-first behavior that no stage in `SKILL.md` performs.
decision: reworded the indicator as an explicit optional manual practice outside the six-stage loop, instead of adding the invitation as a workflow step.
why: adding a new workflow step would have been an architecture change, and the architecture was frozen for the release pass.
tags: [crucible, docs-consistency, scope-control]

### CASE: release-notes-keep-repo-relative-paths
status: auto
problem: three new docs inside `docs/` referenced sibling files with a `docs/` prefix, breaking the established bare-filename convention used by every other doc in that folder.
decision: fixed `ROADMAP.md` and `RELEASE_CHECKLIST.md` to sibling-relative references but deliberately kept repo-relative `docs/` paths in `RELEASE_NOTES_v2.0.0.md`.
why: the release notes are GitHub-Release-facing and may be read outside the repo tree (pasted into the release page), where sibling-relative links would break.
tags: [docs, conventions, release]

### Commands that worked
`winget install --id GitHub.cli` — run by the user in PowerShell to install the GitHub CLI mid-session; confirmed working by the subsequent successful `gh.exe` invocation.
`git push origin v2.0.0` — run by the user; output shown in transcript, tag landed on the remote.
`gh release create v2.0.0 --title "Crucible v2.0.0" --notes-file crucible/docs/RELEASE_NOTES_v2.0.0.md` — executed via the full path `"/c/Program Files/GitHub CLI/gh.exe"` because the shell's PATH predated the install; release confirmed live at the returned URL.
`git push origin main` (user, for the release commits) — UNVERIFIED as verbatim (user reported "pushed manually"; assistant later confirmed the commit was on `origin/main`).

### Problems solved
Claude.ai skill reported ledger/stages missing → root cause: `/mnt/skills/user/crucible/SKILL.md` was a stale v1 copy, not a truncated upload → fix: re-uploaded the current `SKILL.md` from disk; remote grep afterwards found Stage 2 and Stage 6 content correctly.
`gh` command not found after winget install → root cause: the Bash session's PATH was cached before the install → fix: invoked the binary by full path instead of refreshing PATH.
Git pushes denied twice by permission settings → resolved externally: user ran the pushes manually.

### Mistakes
First diagnosis of the claude.ai discrepancy was wrong — it blamed the remote model for misreading its own file or a truncated paste, when the remote model's programmatic grep was accurate; the actual cause was a stale v1 copy in the skill slot. Corrected after the remote's structure description matched v1 exactly.
The three newly created release docs broke the repo's own sibling-filename link convention; caught and fixed during the self-audit pass of the same session.

### Prompts worth keeping
The adversarial teardown prompt ("You are no longer reviewing Crucible v1... Your job is NOT to improve it. Your job is to break it.") — forced abandonment of the ceremony-heavy v2 draft and surfaced the correlated-personas flaw that shaped the final architecture.
The synthesis prompt ("Your job is NOT to choose one. Your job is to determine whether these are actually competing philosophies or whether they should be combined.") — produced the verifiable-now/verifiable-later axis that became v2's core philosophy.

### Questions I still have
Whether the reconstructed EvidenceOS brief used to validate the new Stage 4 matched the real claude.ai session — the real ledger was requested for an exact re-run but never provided in this transcript.

### TODOs for next session
Confirm `993fe4e` (Stage 4 adequacy change) actually reached `origin/main` — the push was blocked for the assistant and the user's manual push is not confirmed in the transcript.
Re-upload `crucible/SKILL.md` to claude.ai so that copy picks up the Stage 4 adequacy methodology (it is one version behind again).
Begin Phase 1 real-world founder testing per `docs/ROADMAP.md` on the released v2.0.0 build.
